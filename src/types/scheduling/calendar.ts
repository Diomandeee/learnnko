export interface ShiftStack {
  shifts: any[];
  width: number;
  left: number;
}

export interface TimeSlot {
  date: Date;
  hour: number;
}

export interface ShiftPosition {
  top: string;
  height: string;
  left: string;
  width: string;
}
