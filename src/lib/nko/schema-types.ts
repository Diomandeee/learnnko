// Only use this if the SchemaType is not exported from the Gemini library
export enum SchemaType {
  STRING = "string",
  INTEGER = "integer",
  NUMBER = "number",
  BOOLEAN = "boolean",
  ARRAY = "array",
  OBJECT = "object"
}
